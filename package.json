{
  "name": "helix",
  "version": "0.0.0",
  "private": true,
  "description": "A 2D hero-combat game for the browser",
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "check": "pnpm typecheck && pnpm build",
    "check:ci": "pnpm typecheck && pnpm build",
    "test": "echo 'pnpm test: not yet. The Vitest tiers are not configured' && exit 1",
    "test:watch": "echo 'pnpm test:watch: not yet. The Vitest tiers are not configured' && exit 1",
    "lint": "echo 'pnpm lint: not yet. ESLint is not configured' && exit 1",
    "lint:fix": "echo 'pnpm lint:fix: not yet. ESLint and Prettier are not configured' && exit 1",
    "typecheck": "tsc --noEmit",
    "dev": "echo 'pnpm dev: not yet. The Vite dev server is not configured' && exit 1",
    "build": "vite build",
    "bench": "echo 'pnpm bench: not yet. The render benchmark scene does not exist' && exit 1"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.3.0"
  }
}
