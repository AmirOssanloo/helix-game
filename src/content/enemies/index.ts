import type { EnemyDef } from "@domain/public";

/** Every archetype, in the order the content tier validates them. An archetype not listed here does not exist; the first arrives with the enemies. */
export const enemies: readonly EnemyDef[] = [];
