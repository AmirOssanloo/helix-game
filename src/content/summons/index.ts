import type { SummonDef } from "@domain/public";

/** Every summon, in the order the content tier validates them. A summon not listed here does not exist; the first arrives with the spell that spawns it. */
export const summons: readonly SummonDef[] = [];
