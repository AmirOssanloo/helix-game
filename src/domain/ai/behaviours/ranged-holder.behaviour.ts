import type { Behaviour } from "../behaviour";

/** The ranged archetypes' driver: it will hold at its attack range less a margin and fire. For now it holds where it spawned, as the stationary driver does. */
export const rangedHolderBehaviour: Behaviour = (): void => {};
