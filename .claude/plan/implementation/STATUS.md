# Status

**Updated:** 2026-09-22 · **By:** the engineer running the plan

Where the plan is right now. One screen. Update it in the same commit as the ticket whose status changed, and move the date.

---

| | |
| --- | --- |
| **Active phase** | [1 — Hero mechanics, camera, and the arena](./phase-1-hero-mechanics/README.md): the gate was walked 2026-09-21 and closes when the person rows below hold; work goes on into phase 2 meanwhile |
| **Active sprint** | [11 — The ten spells, part two, and the phase gate](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md) |
| **Next ticket** | P2-S11-T03 — Emberling |
| **In progress** | none |
| **Last closed ticket** | P2-S11-T02 — Bolide and Clarion, 2026-09-22: the last two spells of the ten, both content alone. Bolide is one zone with a delay, a line motion, and an each-tick list of a per-second damage area and a `burn` reapplied every tick of contact, so a unit the meteor rolls over pays for the ticks it was under it and keeps burning for three seconds after it has passed. Clarion is a damage area, a push away from the hero, and a disarm, each naming the same cone at the caster, all three landing on the commit tick; a unit at the cone's edge is thrown outward rather than along the hero's facing, and one pushed into a wall stops at its edge. Nothing under `src/domain` changed: every piece both spells need landed in sprints 08 and 10. The spells page gains the two edges and says Clarion's cone lands at once. Before it, P2-S11-T08 — The developer panel on a pane, 2026-09-22: the panel's hand-written controls are gone and it is built from Tweakpane. Each group takes the folder it builds into and returns its refresh; a control is a binding over a plain object the group owns, and the overlays bind to the toggles object the play scene reads. A binding reports a change when the panel rewrites it as well as when a person moves it, so every control that follows the world compares before it acts and the refresh touches only those. The tunables lose the default beside each slider and gain a reset that puts every slider a person moved back, one command each. The pane is told to the build as side-effect free, and the strip check now fails on a pane module in the bundle, so the production build is byte for byte what it was before the pane arrived. Ahead of it, P2-S11-T07 — the playable build on GitHub Pages, 2026-09-22: a push to `main` builds and publishes, and the page asks for its bundle beside itself so one build serves from the repository-name path. Before those, P2-S11-T06 — Updraft lifts in place, 2026-09-22: the funnel's carry is gone. `updraft_carry` is now `updraft_catch`, which each tick lifts every enemy inside the travelling zone that the zone has not already taken and records it on the hit list, and does nothing else; no unit's position is written and the spatial hash is told nothing. A lifted unit stands where the funnel found it for the whole of its lift and comes down on that spot, so the drop and its damage stay where they were, in the `updraft_lift` status's expiry list. The zone still travels its distance and still expires with its motion, and the hit list still refuses a second lift when the first ends under a standing funnel. The catalogue, the spells page, and the status-effects page say lift in place, so the approved text and the code agree again |
| **Last closed sprint** | [10 — The ten spells, part one](./phase-2-spells-and-attack/sprint-10-the-ten-spells-part-one.md), 2026-09-22: the on-damage status hook, five spells built from the primitives and the first named effect, and every spell's targeting preview drawn from its own definition in its own colour. Four tickets sized four days landed in 4.0, and the maintainer walked all five spells and all five previews against the dummy |
| **Last closed phase** | [0 — Foundation](./phase-0-foundation/README.md), 2026-09-20: every gate row holds and CI is green on the final commit; the exit record is in the phase README |
| **Last milestone reached** | M3, first spell lands, 2026-09-22: five spells cast against the training dummy with damage numbers, walked by the maintainer in the arena and approved. M1 still waits on the reference laptop |

---

## Waiting on a person

Nothing an agent can do moves these rows. Each one says what to do, where the result goes, and what it unblocks.

- [x] **Sprint 07: the product owner approves the spell catalogue's shape.** Approved as written by the product owner, 2026-09-21: sections 3, 4, and 7 of `docs/product/specs/spell-catalogue.md`, including the three flagged calls. Recorded in a note under P2-S07-T01 in the sprint 07 file. P2-S07-T02 was built against this shape and stands; T03 to sprint 11 build on it.

- [x] **Sprint 06 exit: the draw-call readout against the WebGL inspector.** Done by the maintainer with Spector.js in Chrome, 2026-09-21: the bench at one draw per frame and the game at two, the world's then the HUD's, both matching the readouts. Recorded in the draw-call row of the sprint 06 [exit table](./phase-1-hero-mechanics/sprint-06-developer-panel-replay-and-phase-gate.md#sprint-exit).
- [ ] **Sprint 08 exit: the render benchmark after the icon frames and the icon views.** P2-S08-T04 changed the atlas — three more frames, and every icon frame now a glyph instead of a disc — and added a view kind, `status-icon.view.ts`, whose pool makes 512 quads at scene create. The benchmark needs a GPU and is not in CI, so it is a person's. Run `pnpm bench` on this branch and once on `f618c7e`, the commit before it, and record fps, render ms, draw calls, and heap for each in the render-benchmark row of the sprint 08 [exit table](./phase-2-spells-and-attack/sprint-08-statuses-combat-and-the-primitives.md#sprint-exit). The pass condition is the render budget and a flat heap, in [performance standards](../../../docs/standards/performance.md#the-benchmark-and-the-stress-test). Unblocks: the last row of the definition of done for that ticket. Blocks no ticket.

- [ ] **Sprint 09 exit: the render benchmark after the hit flash and the floating numbers.** P2-S09-T04 changed two view kinds: the unit view now turns its tint mode over for a flash, and a new set of 64 `BitmapText` labels is made at scene create and written while numbers rise. Neither touches the atlas. The benchmark needs a GPU and is not in CI, so it is a person's. Run `pnpm bench` on this branch and once on `2ef33f7`, the commit before it, and record fps, render ms, draw calls, and heap for each in the render-benchmark row of the sprint 09 [exit table](./phase-2-spells-and-attack/sprint-09-projectiles-summons-and-the-auto-attack.md#sprint-exit). The pass condition is the render budget and a flat heap, in [performance standards](../../../docs/standards/performance.md#the-benchmark-and-the-stress-test). Unblocks: the last row of the definition of done for that ticket. Blocks no ticket.

- [ ] **Sprint 11: Updraft in place by hand in the arena.** P2-S11-T06 reversed the carry the maintainer saw on 2026-09-22: the funnel now lifts where a unit stands and moves nobody. Only a person can say whether the spell reads right without the displacement. Open `pnpm dev`, put the panel's **Infinite mana** and **No cooldowns** on, spawn a dummy in front of the hero and a second one further down the same line, and cast Updraft along them. Confirm what the ticket's acceptance asks: each dummy is lifted where it stands and does not slide while it is up, the second is lifted when the funnel reaches it rather than when the first was, and each comes down on the spot it was lifted from with the drop's damage. Record the result in the first row of the sprint 11 [exit table](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md#sprint-exit). Unblocks: the last row of the definition of done for P2-S11-T06. Blocks no ticket.

- [ ] **Sprint 11 exit: the render benchmark and the coalesced numbers in the arena.** P2-S11-T05 changed one view kind: the floating-number set now carries two more arrays over its 64 labels and a hit may join a number rather than spawn one. Nothing touches the atlas and no label is added. The benchmark needs a GPU and is not in CI, so it is a person's. Run `pnpm bench` on this branch and once on `50d1696`, the commit before it, and record fps, render ms, draw calls, and heap for each in the **Bench numbers** row of the sprint 11 [exit table](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md#sprint-exit), which is also the phase 2 gate's. The pass condition is the render budget and a flat heap, in [performance standards](../../../docs/standards/performance.md#the-benchmark-and-the-stress-test). In the same session, cast Glacier at the dummy in `pnpm dev` and confirm what the ticket's acceptance asks: the burn reads as one number a third of a second worth what that third cost rather than one a tick, a single hit still raises its own, and a hit landing beside the burn keeps its number. Unblocks: the last row of the definition of done for that ticket. Blocks no ticket.

- [x] **Sprint 10: the spells by hand in the arena.** Walked by the maintainer, 2026-09-22: Hoarfrost, Quicken, and Zenith each cast at the dummy with the panel open, **Infinite mana** and **No cooldowns** on, and all three behaved as their tickets describe. Recorded in the first row of the sprint 10 [exit table](./phase-2-spells-and-attack/sprint-10-the-ten-spells-part-one.md#sprint-exit). This closes the last row of the definition of done for P2-S10-T01 and P2-S10-T02. Siphon and Wane need the same walk when P2-S10-T03 closes, in a row of their own.

- [x] **Sprint 10: Siphon and Wane by hand in the arena.** Walked by the maintainer, 2026-09-22, and approved: Siphon claimed its circle for the delay, took the dummy's mana and raised a number worth half of it; Wane wore its icon on the hero, slowed the hero's walk, and slowed the dummy standing beside it. Recorded in the first row of the sprint 10 [exit table](./phase-2-spells-and-attack/sprint-10-the-ten-spells-part-one.md#sprint-exit). This closes the last row of the definition of done for P2-S10-T03.

- [x] **Sprint 10: the five previews by hand in the arena.** Walked by the maintainer, 2026-09-22, and approved: each of the five showed the shape, the size, and the colour its definition names, and turned red one unit past the range. Recorded in the first row of the same [exit table](./phase-2-spells-and-attack/sprint-10-the-ten-spells-part-one.md#sprint-exit). This closes the last row of the definition of done for P2-S10-T04.

- [x] **Sprint 10 exit: the render benchmark after the cone frame and the preview.** Run by the maintainer, 2026-09-22, on this branch and on `ad7ba25`, and approved against the render budget with a flat heap, so the box is ticked and the sprint closed on it. The four figures — fps, render ms, draw calls, heap — were not written down, so the **Bench numbers** row of the phase 2 [exit record](./phase-2-spells-and-attack/README.md#exit-record) has nothing to carry into the gate at the end of sprint 11. The sprint 08 and 09 benchmark rows above are still open and are the same command on earlier commits; capture this branch's four figures in that pass and the phase 2 row is answered with them.

- [x] **Sprint 11: Glacier and Updraft by hand in the arena.** Walked by the maintainer, 2026-09-22. Both spells did what P2-S11-T01 built, and two things came back that the tests could not see. Glacier: the burn is applied every tick, which is what the catalogue asks and which makes the total right, but the presentation raises a number per instance, so thirty a second overlap into a block and one burning dummy recycles the whole number set twice a second. Fixed in the presentation alone, as [P2-S11-T05](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md); the domain, the events, and the replays stand. Updraft: the funnel carries the units it lifts, which section 3.5 asked for and the product owner approved on 2026-09-21, and it reads as a knockback and takes the enemy off the screen. The maintainer reversed that call the same day: Updraft lifts in place and the unit comes down where it stood, rewritten in the catalogue and built as [P2-S11-T06](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md). Recorded in the first row of the sprint 11 [exit table](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md#sprint-exit) and under P2-S07-T01 beside the approval it withdraws. This closes the last row of the definition of done for P2-S11-T01; Glacier needs no second walk, and Updraft is walked again when T06 closes, in a row of its own.

- [ ] **Sprint 11: Bolide and Clarion by hand in the arena.** P2-S11-T02 built the last two of the ten, and only a person can say whether they read right. Open `pnpm dev`, put the panel's **Infinite mana** and **No cooldowns** on, and spawn a dummy in front of the hero. Cast Bolide at it and confirm what the ticket's acceptance asks: the meteor claims its circle for the fall, lands after the delay, rolls away from the hero along the line the click made, damages the dummy every tick it is under it, and leaves it burning for three seconds after it has passed. Then cast Clarion along a line of two or three dummies and confirm: a dummy inside the cone is damaged, thrown away from the hero, and disarmed, one wide of the cone or past its length is untouched, a dummy off the cone's axis is thrown outward rather than along the hero's facing, and one with a wall behind it stops at the wall instead of entering it. Record the result in the first row of the sprint 11 [exit table](./phase-2-spells-and-attack/sprint-11-the-ten-spells-part-two-and-phase-gate.md#sprint-exit). Unblocks: the last row of the definition of done for P2-S11-T02. Blocks no ticket.

- [ ] **Sprint 11: turn GitHub Pages on.** P2-S11-T07 added the workflow that builds and deploys on every push to `main`, but the repository still has to be told to take it: open **Settings → Pages** and set **Source** to **GitHub Actions**. Until that is done the deploy job fails and the address serves nothing. Then open `https://amirossanloo.github.io/helix-game/` and confirm the game loads and plays, and that there is no developer panel beside it. Record the result under P2-S11-T07. Unblocks: the last row of the definition of done for that ticket. Blocks no ticket.

- [ ] **Sprint 11: the new panel by hand.** P2-S11-T08 rebuilt the developer panel on a pane. Every control was proved in a browser — the groups render, the readouts run, a spawn and a kill land, and the infinite-mana switch holds against the refresh — so what is left is a judgement only a person makes: whether the panel reads and drives better than the one it replaces, and whether losing the default printed beside each tuning slider is a fair trade for the **Reset tunables** button that took its place. Open `pnpm dev`, work through the groups, and say so. If the default beside each slider is wanted back, it is a ticket, not a fix. Record the result under P2-S11-T08. Unblocks: the last row of the definition of done for that ticket. Blocks no ticket.

- [ ] **Milestone M1: the bench on the reference laptop.** Run by the maintainer on 2026-09-21 and passing by eye; the numbers were not written down, so the box stays open until they are, and the rest of this row says how. Run `pnpm bench` on the reference laptop in Chrome, then in Safari, each twice: once as configured, once with `?textures=default` on the address. Record fps, render ms, draw calls, and heap for each of the four runs in the bench row of the sprint 02 [exit table](./phase-1-hero-mechanics/sprint-02-locomotion-turn-rate-and-the-render-benchmark.md#sprint-exit) and in the bench row of the phase 1 [exit record](./phase-1-hero-mechanics/README.md#exit-record). Unblocks: M1 and one row of the phase 1 gate. Blocks no ticket.

- [ ] **Phase 1 gate: the bar in four browsers on the reference laptop.** Run by the maintainer on 2026-09-21 and passing by eye; the numbers were not written down, so the box stays open until they are, and the rest of this row says how. Open `pnpm dev` on the reference laptop in Chrome, then Firefox, Safari, and Edge; in each, open the panel, spawn 300 units, turn the collision and bound overlays on, walk the hero about for 30 seconds, and take a screenshot of the Readouts group. Record frame rate, tick mean and max, render mean and max, draw calls, pool misses, and view misses per browser in the bar row of the sprint 06 [gate walk](./phase-1-hero-mechanics/sprint-06-developer-panel-replay-and-phase-gate.md#phase-1-gate-walk). In Chrome, also record a 30-second allocation sampler in the performance panel with the 300 units live and note whether the heap is flat. Then run `pnpm test -t "stress"` there and record green, or the failure's mean, in the stress row of the same table and the phase 1 [exit record](./phase-1-hero-mechanics/README.md#exit-record). Unblocks: the bar row, and with it the phase 1 close. Blocks no ticket.

## How to update this page

- A ticket starts: put its ID in **In progress**, and its successor in **Next ticket**.
- A ticket closes: move it to **Last closed ticket**, clear it from **In progress**, and set its `Status` row to `done` in the sprint file.
- A sprint closes: fill the sprint file's **Sprint exit** table, then move the row here and point **Active sprint** at the next one.
- A phase closes: record the gate rows in the phase `README.md`, then move **Active phase**.
- Something waits on a person: write it under **Waiting on a person** as a checkbox with the steps, where the result is written, and what it unblocks. The agent that closes a ticket repeats the open boxes in its report. If it is a decision, it also goes to [Open questions](./backlog/open-questions.md).
