# Open questions

**Written:** 2026-09-20 · **Updated:** 2026-09-25 · **Kept current by:** whoever answers one

Decisions the plan needed and the documentation did not settle. Each has a proposed answer the plan is built on and the ticket that depends on it. Answering one differently means editing that ticket.

---

| # | Question | Proposed answer the plan assumes | Blocks | Status |
| --- | --- | --- | --- | --- |
| Q40 | Five things the P5-S19-T02 ticket leaves open. How does an archetype hold its bash or frost attack for its whole life, when a status needs a duration and nothing applies one at spawn? Where does a projectile an enemy throws at a unit leave from, when a unit cast anchors on its target? Are the four abilities content while no archetype lists them, when Q39 read content as only what plays? What are their numbers? And may a carried status show its icon above the enemy for its whole life? Found building P5-S19-T02 on 2026-09-25 | Decided provisionally on 2026-09-25 by the most conservative reading, with the engineering architect, awaiting the maintainer. An enemy or summon definition gains `statuses`: at most two ids, none raising a flag, that the spawn applies from the unit itself at level one with an end tick no tick reaches; death clears them with the rest of the table. When dispels arrive they must pass over such rows. `spawn_projectile` gains `origin`, `anchor` or `caster`, and every entry written before keeps `anchor`; the net leaves from the caster. The four are content, as Q39 already said of sprint 19's nine, and the content spec no longer asks that an archetype list each ability; the sprint 21 roster assigns them, and no catalogued archetype changes. The numbers are starting values in the definition files: the bash stuns 0.8 s at most once per 4 s; the frost attack slows 1.5 s at most once per 1 s, with the generic 30 % slow; the curse silences 2.5 s with a 0.5 s cast point, range 600, and a 14 s clock; the net is a homing shot at 900 that roots 1.5 s, with a 0.4 s cast point, range 700, and a 12 s clock. The icon shows for life, as a tell: A for the bash, F for the frost attack | Nothing; P5-S19-T02 is built on it | Provisional, awaiting the maintainer |
| Q39 | Five things the P5-S19-T01 selection rule leaves open. When the first listed ability that is ready and in range is refused by the pipeline, a mana shortfall say, does the enemy try the next one or attack? May it start a cast over its own attack point, which a new order cancels? Does it cast on a hero Wane hides from it, when it is adjacent and swinging on? Is a refused enemy request announced, as a refused command is? And is the runbook's `frost_volley` a content ability or a test fixture? Found building P5-S19-T01 on 2026-09-25 | Decided provisionally on 2026-09-25 by the most conservative reading, awaiting the maintainer. The rule submits the first qualifying ability only, as the ticket words it, and a refusal leaves the enemy attacking; the next listed one waits for the first to qualify or leave. Nothing is chosen during the enemy's own attack point, so no swing is lost to a cast, nor while silenced, since the rule is not a command and reads the flags itself. A hidden hero is never cast on; an adjacent melee enemy swings on as the machine already allows. A refusal is not announced, since the enemy would ask again every tick and fill the event ring. `frost_volley` is a test fixture under `tests/helpers/content/`, since no archetype casts it and content holds only what plays; the nine abilities of sprint 19 are content | Nothing; P5-S19-T01 is built on it | Provisional, awaiting the maintainer |
| Q38 | What does the P4-S17-T04 balance pass measure, and what may it change? The ticket asks for three sessions "recorded with the panel" and for the grunt to be kited, the runner not, the archer to punish standing still, and the tank to take a combo, and says the findings go into the definitions. No page says how to tell whether each holds, how far a finding may move a number, or what a changed number with no patch citation cites. Found building P4-S17-T04 on 2026-09-25 | Decided provisionally on 2026-09-25 by the most conservative reading, awaiting the maintainer. The sessions are played headlessly by a script that sends only what the panel and a player send (spawns, levels, orb levels, the two switches, heals, map reloads, moves, attacks, orb keys, Invoke, casts), so each log loads in the panel like one a person saved. Each goal is a check on the log: the grunt is kited when a level-1 hero walking a circle 800 across and shooting only at a gap of 300 kills one without being hit (68 s); the runner is not when it catches that walk (ten hits in 20 s); the archer punishes standing still when standing and trading takes more than one and a half times the health a second walking away does (9.1 against 4.4); the tank takes a combo when at orb level 7 no spell alone takes half its health and Updraft, Zenith, Bolide, and Clarion in turn kill it. All four held on the numbers as they stood. A number moves only where a measurement contradicts a reason a catalogue gives, so one moved: the archer's mana, 200 to 550, Siphon's largest burn, because at 200 Whorl 3 to 7 burned no more than Whorl 2 from the one archetype carrying mana, against the catalogue's "a mana pool gives Siphon something to take from a real enemy". Left as they are and named for the maintainer: the grunt's 240, kited but slowly; the hero's attack, which does not grow with level, so a pack of five is a fight for spells at every level and the attack alone clears only runners, from level 10 (giving it growth is a new conversion, not a number); Zenith's split against a pack and Glacier at level 1, both the source's shape. A number with no patch citation cites the pass alone, `// tunable; balance pass 1`. "The catalogues match the definitions" is checked by a new content spec over both catalogues' tables. A number change re-stamps the stored logs whose specs do not rest on the number and records the balance logs again | Nothing; P4-S17-T04 is built on it | Provisional, awaiting the maintainer |
| Q37 | Three things the P4-S17-T03 ticket leaves open. Where does a log keep the second version, when the file has one stamp and the two stored replays carry it? Is a log whose reloads came back to the version it began on still two versions? And does a reload that changes no number mark the log? Found building P4-S17-T03 on 2026-09-24 | Decided provisionally on 2026-09-24 by the most conservative reading, awaiting the maintainer. `contentVersion` is the version the world was created under, and a new `contentReloads` lists each version a reload moved the session to; any entry refuses the log, the message naming every version in order. A reload back to the starting version is still refused, although the log's own tuning commands would reproduce it, because R14 says a replay is only valid within one content version. A reload that leaves the stamp where it was marks nothing. A recreate or a load starts a log on one version again | Nothing; P4-S17-T03 is built on it | Provisional, awaiting the maintainer |
| Q36 | Four things the P4-S17-T02 ticket leaves open. What does a content edit that changes more than numbers do, such as a renamed key, a colour, a map, or a definition added? How does a reload tell a number the person tuned from one it may replace? How does the new number reach a running world, when ADR 0004 says tunables change no other way than by command? And what happens to a reload while a replay runs? Found building P4-S17-T02 on 2026-09-24 | Decided provisionally on 2026-09-24 by the most conservative reading, awaiting the maintainer. Only a registry that differs in numbers the tuning surface reaches is taken in place. Anything else, a colour and the step rate included, reloads the page, because the tuning state never grows and a world cannot be patched mid-tick. A number counts as the person's when the world holds anything but the old default for it, compared converted; a slider set to exactly the default cannot be told from no slider and takes the new default. Each changed number goes in as a `set_tuning` command for the next tick, so it is in the log. The commands go to the session, not the driver, so a hidden tab holds them rather than dropping them as it drops input. A reload while a replay runs is refused on the content line and the replay keeps its content, per R14. The session's stamp moves to the new version; marking a log that spans two versions is P4-S17-T03's. The boundary is the composition root accepting `content/public.ts`, the door it imports content through, rather than `content/index.ts` itself | Nothing; P4-S17-T02 is built on it | Provisional, awaiting the maintainer |
| Q35 | Four things Q5's key shape and the P4-S17-T01 ticket leave open. Where is a missing-key type error checked, when the domain may not import content and the panel builds its keys at run time? How does a key name a number inside a list of objects, such as an effect list? What id does the hero, which has none, go by? And is a colour, a number on every spell and archetype and marked `// tunable`, on the surface? Found building P4-S17-T01 on 2026-09-24 | Decided provisionally on 2026-09-24 by the most conservative reading, awaiting the maintainer. Content derives the exact key union from its own constants, `ContentTuningKey` in `src/content/content-tuning-key.ts`, and a hand-written command typed `ContentTuningCommand` fails to compile on a key that names nothing. The domain's command type checks the `def:<kind>:` shape only, and the world refuses a key it does not hold, as it refuses a hand-edited tuning table key. An entry of a list of objects is a dot segment of its own, `def:spell:hoarfrost:effects.0.seconds.byLevel:2`, keeping the colon for a table of numbers only. The hero is `def:hero:hero:…`. Colours are left off, since a slider over a packed RGB integer moves nothing a designer means; they fall under the deferred row for non-numeric fields. The unit a field converts from is read from its name, as the coding standard names fields. Records are rebuilt from the world's own copy of a definition when a key of it is tuned, not read through the tuning state by string: R13's copy-on-create, not a live proxy | Nothing; P4-S17-T01 is built on it | Provisional, awaiting the maintainer |
| Q34 | What is "wedge sweep smoothing", which key names each feedback timing, and does a flash already showing take a new length? The P4-S16-T03 ticket lists "wedge sweep smoothing" among the feedback timings to make tunable. The presentation page says a wedge changes frame once per step of a 64-frame sheet and names no smoothing, and no page names the tuning keys or says when a changed timing applies. Found building the tunables on 2026-09-24 | Decided provisionally by the agent on 2026-09-24, awaiting the maintainer, on the most conservative reading. Smoothing is how many steps the wedge sweeps in, `cooldown_wedge_steps`, 64 by default, which is the sheet's own count and draws exactly what was drawn before; fewer steps is a coarser sweep drawn with the sheet's nearest frame, and a count past the sheet's is held at it, since there are no finer frames to draw. The keys are `hit_flash_duration` 0.133 s, `refusal_flash_duration` 0.333 s, `damage_number_rise` 56 pixels, `damage_number_fade_duration` 1 s, and `camera_follow_lerp` 0.1, each the value that was a constant in presentation, so nothing on screen changes at the defaults. Rise and fade are one life, as they were: a number rises and fades over the same ticks. A flash or a number takes its length when it starts, so one already showing keeps it, as the HUD page says a joined number keeps its rise; a rise, the wedge's steps, and the lerp are read each frame. The floating-number set stays sized for a one-second life, so a longer life recycles sooner and counts it. The alternative for smoothing is interpolating the wedge between frames, which the one-frame-per-step rule of the presentation page rules out | P4-S16-T03 | Provisional, awaiting the maintainer |
| Q33 | Which colour is each damage type, and where does the table live? The HUD page said numbers are white until the balance pass needs a colour per type, and that the colours would make the merge window one per unit per type. It names no colours. The sprint 16 ticket asks for "a small tunable table" and says crit styling stays deferred. The sprint risk says presentation never reads the content module. Found building the colours on 2026-09-24 | Decided provisionally by the agent on 2026-09-24, awaiting the maintainer. Physical is red `0xff4d4d`, magical is blue `0x4fc3f7`, and pure is gold `0xffd54f`: the usual reading of the three, with no two of them and none of them against the white hit flash closer than 128 summed over the channels. The table is `DAMAGE_NUMBER_TINTS` beside the floating-number set in `src/presentation/views/`. It is a presentation number tuned in the file, like the rise and the merge window, and not a tuning-table key, because a colour changes nothing in the simulation and the log. The merge window is one per unit per damage type, as the HUD page foresaw. The alternative is putting the colours in the tuning table with panel controls beside the feedback timings of P4-S16-T03 | Nothing. A different colour is one line | Provisional, awaiting the maintainer |
| Q32 | What does a lifted unit do with a push, with a unit walking into it, and with a spell aimed at it? The status page says a lifted unit is raised, stunned, untargetable, and "moves nowhere", and that under a root it "comes down on the spot it was lifted from". It does not say what happens to a push that lands in the same tick as the lift, whether a lifted unit is still a disc other units collide with, or what happens to a unit-target spell aimed at one. As built before P4-S16-T01, a push carried a lifted unit, a walker shoved it off its spot, and Hoarfrost thrown at one walked, cast, and spent its mana for a status that could not land. Found writing the displacement edge cases on 2026-09-24 | Decided provisionally by the agent on 2026-09-24, awaiting the maintainer, on the most conservative reading of "moves nowhere" and "untargetable". Lift wins over a push: a push on a lifted unit counts its ticks off without carrying it, so the rest is spent in the air. A lifted unit is still a disc, but nothing moves it: the unit on the ground takes the whole overlap. It is not in no pair at all, so walkers pass under it, because that let the corridor-200 crowd flood through where the hero hung and pressed a pair to 0.80 of its summed radii, past the 0.75 bar. A unit-target spell at a lifted unit is refused as `target_untargetable`, and a target lifted during the approach or the cast point cancels the cast at no cost, as a target that dies does. Written into the status, spells, and movement pages. The alternative for walkers is to pass under a lifted unit | P4-S16-T01 | Provisional, awaiting the maintainer |
| Q31 | Should a crowd shove the hero, and how much overlap may a pressing crowd show? Push-out is positional and even, half each, so two hundred chasers walking into the hero every tick push it as hard as it pushes them. In the corridor session of P3-S15-T04 the hero, ordered back to its spot every half second, was carried from the corridor at (3000, 2000) to (3864, 2622) in fifteen seconds and dragged grunts past their leash; the stress spec's hero is carried nearly 400 units off its loop. Under the press, capped passes also leave overlap every walker renews each tick: at four passes the worst pair over the session is 68% into each other and the median tick's worst is 17%. More passes shrink it, each costing about a third of collision's 0.27 ms as a bundle at three, P3-S15-T01's figure (six give 54%, eight 37%), and sweeping in alternate directions, sorting by distance to the hero, or pushing out of walls inside the pair loop each did little. Found building the corridor test on 2026-09-24 | Decided provisionally by the agent on 2026-09-24, awaiting the maintainer, on the most conservative reading: the rule stays as the movement page states it, even push-out with no speed change and enemies that push, never steer, and the hero is a disc like any other. Only the pass cap moved, from three to four, which is the smallest that stops a press putting two discs on one point, three having let two runners in the corridor sit 99% into each other. The alternatives are a design change, not a tuning: a unit standing still, or the hero, taking a smaller share of a push than a unit walking into it, or a walk step that stops at contact. Both would change how a pack feels in the corridor and belong to the balance pass or an ADR | Nothing. The sprint 17 balance pass took neither up, and its four goals held with the push rule and the cap of four as they stand (Q38); the pass cap still costs collision 0.31 ms of the 0.52 ms tick at the phase 4 cap. A change to the push rule is an amendment to ADR 0002 and the movement page | Provisional, awaiting the maintainer |
| Q30 | Is the tick's steady-state allocation "zero" in the code or zero on the collector? The standard bans literals, closures, and array methods in a system, and the code has none. But V8 boxes a fractional number passed to a call it does not inline, and the sampler reads that as allocation. P3-S15-T01 took the tick from about 100 KB to about 13 KB of it at two hundred enemies by passing the spatial hash points as objects. Most of what is left is movement handing plain numbers to its pure rules: `turnToward`, `isInsideCone`, `movementSpeed`. Found profiling on 2026-09-24 | Decided provisionally by the agent on 2026-09-24, awaiting the maintainer, on the most conservative reading that keeps the docs' other rules: zero in the code, and the engine's boxing is held down where a call is made per unit per tick through a shared door, which is the rule now written in the simulation coding and performance standards. The pure rules keep their plain-number arguments, since the same standard wants a rule testable with three arguments. What is left is young-generation garbage only, a scavenge every forty-odd seconds, and the heap stays flat. The alternative is rules that take the unit, or write into an out object, which would bring the tick to about zero on the sampler. Measured again by the agent on 2026-09-25 in P4-S18-T01, at the phase 3 scenario plus twenty zones: the damage door was a shared door boxing per burning unit per tick, 4.6 KB, and now takes a record; the tick went from 12.7 to 7.9 KB, of which movement's pure rules are 4.1 KB (8.6 KB without the zones), and the reading stands | Nothing. T02's stress test asserts the tick time, not the bytes | Provisional, awaiting the maintainer |
| Q29 | How big is a diamond in the floor art? As built, the floor draws one diamond per 32-unit walkability cell, 40 by 20 pixels at the chosen scale, which leaves too little room to paint detail in. Raised by the maintainer on 2026-09-23 on reading the floor-tile request | One art diamond per four by four walkability cells: 160 by 80 pixels, 128 world units a side, at the same scale, so the view chosen in Q27 stands. Chosen by the maintainer on 2026-09-23, the size of the Diablo 2 floor diamond they compared it with. The art keeps the 2:1 shape the projection draws; a 3:2 diamond was considered and ruled out because its lines would run against every obstacle edge. Zooming in for larger cells was ruled out because it gives up the overview Q27 chose. The walkability overlay draws a finer grid than the art, sixteen cells to a diamond | P3-S24-T02, the floor PNG | Settled 2026-09-23 |
| Q28 | How fast does an enemy heal while it walks home? The enemies page says Return regenerates and gives no rate. Nothing else regenerates an enemy, so as built it heals at its definition's own rates only while returning: a grunt at 1 health a second recovers about 25 of its 400 on a 1500-unit walk, and a kited pack comes back nearly as hurt as it left. Found building the state machine on 2026-09-23 | As built, the definition's rates, since they are the only numbers content gives and the catalogue already reasons about them. The alternative is a tunable fraction of maximum health per second while returning, the source game's leash heal, which makes pulling and resetting a pack free; that is a design call for the product owner, and it is one tunable and one line in the Return step | Nothing. Sprint 13's death and experience tests read either | Open |
| Q27 | Which scale does the isometric view keep? Three candidates, each a 2:1 diamond per 32-unit cell at zoom 1: 48 by 24, 44 by 22, and 40 by 20, showing about 1810, 1975, and 2172 world units across against today's 1920. Raised by the maintainer on 2026-09-23 with the request to start slightly zoomed out from today | 40 by 20, k = 0.625, chosen by the maintainer on 2026-09-23 after walking all three in P3-S23-T01: it shows about 2172 world units across, the widest overview, and makes movement easiest to plan. Whole-pixel diamonds, and a floor tile of 160 by 80 for four by four diamonds. The proposed 44 by 22 was not taken | P3-S23-T02, P3-S23-T03, the floor PNG | Settled 2026-09-23 |
| Q9 | Does the boss encounter run at 200 enemies plus a boss and adds, or lower? | The number is decided by the phase 4 headroom table, read on the worst browser's measured max tick, never the mean, because the budget is a ceiling. If the margin is under 1 ms, the cap is written lower in the phase 5 README and the ticket says so. Settled on 2026-09-20: the boss and its adds are enemies, so they count inside the cap; the table decides whether 200 holds, not whether 200 plus some number holds. Settled further the same day, with the engineering architect: the cap is a named domain constant, `ENEMY_LIVE_CAP`, declared beside the unit pool's capacity in the unit file and asserted at module load to fit inside it with the hero and a summon allowance. It is a budget number, not a designer number, so it is no tuning key. One spawn function in the domain enforces it by counting live enemy units before acquiring; the panel's spawn and the boss's summon adds both refuse through that path. A spawn is an event, not a per-tick cost, so the count is a scan until a profile asks for a counter. The projectile figure of 100 is a measurement condition, not an enforced cap. So that a lower cap is computed rather than guessed, the headroom table carries a second tick row at 100 enemies to give the per-enemy slope, and the cap becomes the largest multiple of ten whose projected max tick leaves 1 ms. The constant and the spawn path arrive with the first spawn command. Settled on 2026-09-21: the constant is written as 200, the target, when it arrives with the pack spawn in sprint 12, so the phase 3 stress test exercises the real ceiling and nothing waits on the measurement; sprint 18 rewrites it from the table. Decided provisionally on 2026-09-25 by the agent in P4-S18-T04, awaiting the worst browser's figure: the cap stays 200. The headroom table's worst tick at 200 enemies is 2.33 ms headless in V8, 1.67 ms of margin, and the rule lowers the cap only under 1 ms; the slope, 0.0036 ms an enemy on the worst tick, leaves 1 ms up to about 380. The worst browser's max tick waits on a person on the reference laptop, deferred until phase 5 is done by the maintainer's standing instruction of 2026-09-24; when it is read, a figure M over 3 ms makes the cap the largest multiple of ten not above 200 + (3 − M) ÷ 0.0036. The decision and the formula are written in the [phase 5 README](../phase-5-full-enemy-roster/README.md#live-cap-for-the-boss-encounter) | P5-S21-T04 | Provisional at 200, awaiting the worst browser's max tick |
| Q25 | Does a status under the stack rule keep one row per applier or one row with a count? The status page gives "damage over time from different sources" as the example of stacking, which reads as one row per source; the table is keyed by status id, so the rule as built keeps one row and counts it, and the row's source and orb levels are the newest applier's. Found building the status system on 2026-09-22 | One row with a count, as built. Nothing in the catalogue stacks: every status definition refreshes or ignores, and the meteor's burn refreshes on every tick of contact by design, so no content tells the two apart. One row per source costs a row of the eight-row table per applier and a second pass to sum them, and the answer wanted is whichever the first stacking status needs. If it is per source, the change is the table key, not the system | Nothing. No status stacks until one is written | Open |
| Q24 | The word "effect" names two concepts. The vocabulary says an effect is a short-lived visual with no rules of its own, and `src/domain/entities/effect.ts` is that pool; but an ability definition's `effects` list, `EffectDef`, the named-effect registry, and the effect runner all name what a cast does, which is nothing but rules. Found writing the effect runner on 2026-09-22 | Keep both words and let the vocabulary say which is which, since ADR 0005, every architecture page, the catalogue, and the definition types already read "effect" for the list, and the visual pool has no user yet. The alternative is renaming the visual entity, which is cheap now and dear once views draw it; if it is renamed, the moment is before sprint 09 spawns the first one | Nothing. Sprints 08 to 11 write effect lists either way | Open |
| Q23 | Do the `collision_radius`, `bound_radius`, and `selection_radius` sliders reach the hero? The hero's body comes from its form definition, which carries the same three numbers marked tunable, and a unit reads the tuning table only when it is acquired, so today a slider move changes units spawned afterwards and never the hero standing there. Found walking row 12 of section 15 on 2026-09-21 | Either the stats system re-wears the body each tick from the form definition and the tuning table, the tuning table's three keys are dropped in favour of the definition's, or the form definition's three numbers are removed and the hero reads the table like every unit. The last is the plan's assumption, since the spec's section 17 lists the three as tunables and one word per number is the rule | Nothing. The sprint 17 balance pass moved no radius and did not need the answer, and the definition tuning surface of P4-S17-T01 reaches the form's three numbers as `def:form:skein:…` keys with the same catch: the hero copies them onto its unit when it is made, so a retune reaches the next hero, not the one standing there. The second tuning path makes the plan's assumption more pressing, not less | Open |
| Q26 | Where does a duration an effect list writes in seconds become ticks? Every other second-to-tick conversion happens once at world creation, in a record beside the definition — a spell's cast point, a status's damage — and both the simulation loop page and the definition of done say a system never multiplies by the tick rate. An effect list has no record of its own: the primitives read the definition entry the effect runner hands them, so a duration on an apply-status, a displace, or a zone entry is converted when the effect runs. Found building the primitives on 2026-09-22 | One door, as built: `ticksOfSeconds` in `src/domain/definitions/`, the tier that owns every other conversion, read at the same fixed step rate, so the arithmetic is in one place and a list gives the same ticks every run. The alternative is an effect-record tier mirroring `EffectDef` with every duration and rate precomputed per orb level, which the runner and every primitive would read instead of the definition. That is the right shape if a zone's each-tick list ever shows on a profile, and it is one function to replace; it is not worth writing before the zones, projectiles, and ten spells that would have to be threaded through it exist | Nothing. Sprints 08 to 11 read the one door either way. The condition the alternative waited on has been measured: in P4-S18-T01's profile at two hundred enemies with twenty zones standing, the zone pass is 0.02 ms of a 0.52 ms tick, so the one door stands on the numbers | Open |

---

## Answered

Move a row here with the date and who answered it when it is settled.

| # | Answer | Date | By |
| --- | --- | --- | --- |
| Q13 | No blank line between import groups. Built-in, external, aliased layers, relative; alphabetical inside each group. The coding standard is updated to say so | 2026-09-20 | The maintainer |
| Q14 | `.only` is banned. `.skip` needs a same-line comment; lint checks that it exists and review checks it names an owner and a condition. No ticket ids in code | 2026-09-20 | The maintainer |
| Q15 | Phaser is stubbed through a Vitest alias to `tests/helpers/doubles/phaser-stub.ts` in every project. `vi.mock` is banned across the tests tree with no carve-out | 2026-09-20 | The maintainer |
| Q16 | `no-console` is an error under `src/**` except `src/app/**` and `src/devtools/**`, where `warn` and `error` are allowed. Amended the same day in sprint 01: `src/app/**` also allows `log`, because presentation may not write to the console and the scenes report through a callback the composition root supplies; `src/devtools/**` stays at `warn` and `error` | 2026-09-20 | The maintainer, amended by the engineering architect |
| Q18 | Definition fields are camelCase, as every property is. snake_case is for string values only: ids, effect keys, behaviour keys. A tuning key keeps each segment's own case, so its field path is the property name verbatim and the compiler catches a stale key. The coding standard's naming table, the content standard's example, the two runbooks, and the simulation coding example are updated; Q5's proposed key shape records the path form | 2026-09-20 | The maintainer |
| Q1 | A summon expires on the same tick its owner dies, for hero summons and enemy adds alike. The expiry goes through the death system as an expiry and grants no experience. The ability pipeline page is corrected with the answer, not in P2-S09-T02 | 2026-09-20 | The maintainer, with the engineering architect |
| Q2 | A `begin_channel` debug command puts the hero in `channeling` for N ticks. It changes world state, so it is a command and replays. It enters the state through the order state machine so AT-O4 exercises the real abort path, and it stays after real channels exist as the cheapest way to reach the state in a test | 2026-09-20 | The maintainer, with the engineering architect |
| Q3 | A status capability. `StatusDef` carries `onDamageTaken` and `onDamageDealt` hook keys or null, resolved at registry build from `src/domain/combat/hooks/`, one file per hook, file name is the key. Hooks run inside the damage function after mitigation; damage caused by a hook runs no hooks. The internal cooldown is a ready-at tick on the status table entry, its length on the definition. Amended in sprint 07: a hook carries an effect list and a cooldown table instead of a key, since the list is the whole behaviour and the effect runner already runs one, so there is no `src/domain/combat/hooks/` and no registry for it. Everything else in the answer stands | 2026-09-20, amended 2026-09-21 | The maintainer, with the engineering architect |
| Q4 | No spike. Phaser 4.2.1 has no counter, but every batch handler, the filter pass, and the GPU tile layer draw through two public renderer methods, `drawElements` and `drawInstancedArrays`, and the renderer emits pre-render, render, and post-render events. A presentation module wraps the two methods on the renderer instance at boot, resets on pre-render, writes the count to an instrumentation ring on post-render, and splits it per scene from the render event so the world figure excludes the HUD. Under the Canvas renderer the readout shows a dash. Risk R10 retires | 2026-09-20 | The maintainer, with the engineering architect |
| Q5 | `def:<kind>:<id>:<field path>[:<index>]`, numeric fields only, the field path being the property path verbatim with a dot per nesting level and a colon before a table index. The index is the array index verbatim, so `:2` is level 3, and the panel labels it "level 3" beside the key. The log and the panel carry the field's designer units; the registry converts once at world creation and once more when a tuning command is applied, so systems read ticks and the key names the source field | 2026-09-20 | The maintainer, with the engineering architect |
| Q6 | The mapper emits nothing on a plain left click and consumes the click so it can never become a move. Selection is presentation state, so no command variant is added | 2026-09-20 | The maintainer, with the engineering architect |
| Q7 | The `aggro_hidden` flag is computed by the status system early in the tick beside the disable flags and tested in sprint 10. The aggro behaviour reads flags, never the status table, and the "drop existing aggro unless adjacent and attacking" half is a behaviour rule tested in sprint 12 | 2026-09-20 | The maintainer, with the engineering architect |
| Q8 | The stubs get their final ids with placeholder effect lists. A rename in phase 2 would invalidate every phase 1 replay. The spec's section 11.5 carries an amendment saying so | 2026-09-20 | The maintainer, with the engineering architect |
| Q10 | The product owner, within the sprint, approves the shape: the effect lists, the primitives, and the capability list. That blocks the schema ticket. The numbers are starting values the tuning surface changes at no code cost, so they never block. A discussion beyond the day is calendar time outside the sprint | 2026-09-20 | The maintainer, with the engineering architect |
| Q11 | No. Phase 2 stays whole. The three-spell cut leaves projectile, summon, and lift unexercised by hero spells when the AI module is written, and enemy abilities in phase 5 reuse those primitives. The re-cut stays described in [Deferred](./deferred.md) for leadership | 2026-09-20 | The maintainer, with the engineering architect |
| Q12 | Pause, single-step, and the catch-up cap are driver operations on `DevApi`, not commands. They never change world state, and a replay runs in Node with no driver, so a pause in the log would be a no-op the world had to learn to ignore. ADR 0004, the devtools architecture page, and the developer panel page are corrected with the answer | 2026-09-20 | The maintainer, with the engineering architect |
| Q19 | Where does a fresh session's seed come from: the composition root draws it from the wall clock at boot, which the app layer may read, and passes it to `createWorld`. The input log header carries it, so a replay recreates the world under it. The panel's seed control recreates the world under a chosen seed; that is a driver operation on `DevApi` like pause, not a command, because it makes a world rather than mutating one. Until sprint 06 every session boots on seed 1 and the composition root says so; P1-S06-T03 removes that | 2026-09-20 | The maintainer, with the engineering architect |
| Q17 | The composition root may import Phaser to construct the game and hand it the scenes, never to build a view or read a game object. Lint already allows `src/app/**`. The layers page, the presentation page, the presentation coding standard, the tech stack page, and the lint message are corrected with the answer, not in sprint 01 | 2026-09-20 | The maintainer, with the engineering architect |
| Q21 | One percent per Whorl level per held instance, multiplicative across instances, as the `whorl_cdr_per_instance` table in the tuning table beside the speed table. Each held instance writes a `cooldown_reduction` modifier row and the pipeline snapshots the rows at commit, so a later item joins by writing a row. It is a designer number, not a decision: the sprint 17 balance pass retunes it through the tuning surface at no code cost, so it blocks nothing. Three instances at level 7 leave about four fifths of a clock, a sane band for a prototype. Nothing in the code changed | 2026-09-21 | The maintainer, with the engineering architect |
| Q22 | The lowest level among the orbs in the recipe indexes a spell's cooldown and mana tables, decided by the cast pipeline at commit; the cooldown module takes the level and decides nothing. Averaging the recipe's levels and flooring plateaus on the rounding and lets a level 7 and level 1 pair read a mid-table cooldown on a spell whose weak half barely acts. Per-field orb attribution, the source game's model, changes the definition schema, which Q10 leaves to the sprint 07 shape review; it can be added there for effect magnitudes without touching this index. The lowest is monotone in every orb and needs no arithmetic; the cost is that the three-orb spell scales last, which its tables absorb. Decided before sprint 06 records a replay, since changing the index changes cooldown ticks. Nothing in the code changed | 2026-09-21 | The maintainer, with the engineering architect |
| Q20 | Cancelled, not refused. A move, attack, or cast issued during an attack point or a cast point cancels the point on the tick the command is consumed, spends nothing, starts no clock, and lands as the new order; nothing is queued. The source game does the same, kiting depends on the attack-point cancel, and a dropped click is worse than either a cancel or a queue. The order state machine's four issue functions land from every state, `beginChannel` lands from an attack point, and the validator reads disable flags only. The controls, ability pipeline, casting flow, spells, commands, and simulation coding pages and the spec's section 13 are corrected with the answer. Built as P1-S05-T04 before sprint 06 records a replay | 2026-09-21 | The maintainer, with the engineering architect |
